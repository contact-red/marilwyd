# Repository Code Review

- **Date:** 2026-08-21
- **Reviewer:** ChatGPT
- **Target:** Complete tracked `HEAD` at `9b487e0`
- **Mode:** Full standalone review using all nine `pony-code-review` personas
- **Baseline:** Pony 0.69.1 compiled, all 260 tests passed, and `pony-lint` passed both packages

The 41 consolidated findings cover six recurring boundaries: contracts,
identity, sync state, delivery and transactions, resources, and parsing.

## Critical
None.

## High
- **Location:** `Makefile:91`, `marilwyd/config.pony:101`, `marilwyd/config.pony:107`. **Finding:** [Contracts] The normal run target makes optional bridging mandatory, while help misstates credential format and bridge topology. **Personas:** Correctness, Principles, Editor. **Evidence:** `make run` always passes an untracked `bridges.yaml`, so a documented non-bridge setup fails; help calls generated/parsed YAML JSON and describes one shared Matrix room instead of per-user rooms. **Fix:** Make `BRIDGES` conditional, call credentials YAML, and describe the per-user bridge-room model.
- **Location:** `README.md:301`, `marilwyd/session_registry.pony:10`, `marilwyd_test/session_tests.pony:566`. **Finding:** [Contracts] Session prose falsely says requested device IDs are never reused, and the implemented resumption branch lacks a positive regression test. **Personas:** Editor, Correctness, Principles (false contract); Tests (coverage gap). **Evidence:** `_device_for` resumes an existing same-account ID and revokes its prior token; the only requested-ID test covers a deleted ID, so unconditional reminting would pass. **Fix:** Document same-account resumption versus deletion, then test queue preservation, old-token rejection, and cross-account refusal.
- **Location:** `marilwyd/account_data.pony:45`, `marilwyd/account_data.pony:112`, `marilwyd/routes.pony:153`. **Finding:** [Identity] Account-data routes ignore `:userId`, so the path names one account while the token silently accesses another. **Personas:** API Design (defect), Tests (HTTP coverage gap). **Evidence:** GET/PUT decode only `:type` and always use `session.user`; Alice can use Bob's path and receive Alice's data, and neither route is tested through HTTP. **Fix:** Validate the decoded path owner against the session and return `403 M_FORBIDDEN`; add real-socket PUT/GET/sync and cross-account no-mutation tests.
- **Location:** `marilwyd/config.pony:237`, `marilwyd/routes.pony:75`, `SECURITY.md:31`. **Finding:** [Identity] Descendant symlinks in the public asset tree can disclose configured credentials or bridge data outside it. **Personas:** Security. **Evidence:** Startup checks only configured pathnames; the checked-in SECURITY probe establishes that the static-serving dependency follows asset-tree symlinks, so `asset_root/leak -> /outside/credentials.yaml` bypasses the overlap guarantee. **Fix:** Serve an immutable tree, reject symlinks recursively, or use descriptor-relative no-follow opens; test links resolving to configured secrets.
- **Location:** `marilwyd/config.pony:246`, `marilwyd/config.pony:304`. **Finding:** [Contracts] Asset containment uses a character prefix and currently rejects safe sibling paths such as `/srv/element-private` for root `/srv/element`. **Personas:** Adversarial (defect), Tests (boundary coverage gap). **Evidence:** Both checks call `resolved.path.at(asset_root.path, 0)` without a component boundary. **Fix:** Compare canonical components or exact root/root-plus-separator; test sibling-prefix and true-descendant cases for both files.
- **Location:** `marilwyd/config.pony:257`, `marilwyd/config.pony:312`. **Finding:** [Identity] Configuration checks permit group/world-writable credential and bridge files. **Personas:** Security (write/TOCTOU defect), Tests (missing `Configure` boundary coverage). **Evidence:** Read bits are rejected but modes such as `0620` and `0602` are accepted, allowing local replacement before restart; pathname-check-then-open adds a replacement window. **Fix:** Open once, verify regular-file ownership and all read/write permissions/ACLs, parse that descriptor, and test write-only modes, ownership, replacement, readability, and inside-root refusal.
- **Location:** `marilwyd/credentials.pony:65`, `marilwyd/_decoy.pony:24`, `README.md:308`, `SECURITY.md:72`. **Finding:** [Identity] Per-entry PBKDF2 counts make wrong-password timing distinguishable from the fixed-cost unknown-user decoy, contradicting the anti-enumeration guarantee. **Personas:** Adversarial, Security, Editor (defect/false contract); Tests (coverage gap). **Evidence:** Entries accept 100,000 or more iterations while `_Decoy` always uses 600,000; current tests compare response bytes only and would pass with no decoy work. **Fix:** Equalize bounded online verification work, define migration for outliers, narrow prose until true, and deterministically test work across unknown, wrong-password, and varied-count entries.
- **Location:** `marilwyd/device.pony:192`, `marilwyd/device.pony:248`, `marilwyd/room.pony:284`. **Finding:** [Sync] Invitations are permanent unpositioned state: one invite disables long polling, repeats with the same token, and cannot be declined despite success. **Personas:** Correctness, Adversarial, API Design, Wildcard (defect); Tests (notification gap). **Evidence:** Invitation arrival does not advance `_position`, nonempty `_invites` always answers, and invitee leave mutates only members, retaining room/user/device invite state. **Fix:** Separate durable invite state from positioned arrival/withdrawal transitions, implement invitee withdrawal, and test one-shot delivery, reparking, leave removal, and lost join permission.
- **Location:** `marilwyd/device.pony:239`, `marilwyd/stream_position.pony:54`. **Finding:** [Sync] A future same-epoch `since` token acknowledges unseen data. **Personas:** Correctness (defect), Tests (coverage gap). **Evidence:** The parser accepts any current-epoch index and `Device.sync` acknowledges room/to-device queues before comparing it with `_position`, so `s<epoch>_MAX` deletes pending data and suppresses recovery state. **Fix:** Normalize future indices to `None` before acknowledgment and test parser cases plus recovery with pending room and to-device data.
- **Location:** `marilwyd/device.pony:374`, `marilwyd/ephemeral.pony:112`, `marilwyd/room.pony:460`. **Finding:** [Sync] The last typing-clear transition is acknowledged but never sent. **Personas:** Correctness, API Design, Wildcard. **Evidence:** `Room` publishes empty typing, `_ephemeral_since` drops empty combined state, and rendering omits `m.typing` when the list is empty; the existing test covers initially empty state, not nonempty-to-empty. **Fix:** Represent update presence separately, emit `user_ids: []` exactly once, clear typing on leave, and add a position-aware true/sync/false/sync test.
- **Location:** `marilwyd/login.pony:45`, `marilwyd/credentials.pony:65`, `SECURITY.md:133`. **Finding:** [Resources] Anonymous login requests can monopolize every Pony scheduler thread with non-preemptible PBKDF2. **Personas:** Security, Performance. **Evidence:** Every validly shaped attempt performs 600,000 iterations without admission/rate limits; checked-in measurements report about 380 ms occupancy and unrelated static latency above one second under eight requests on two threads. **Fix:** Use a bounded native-thread/process executor and admission queue, enforce global/source/account limits with Matrix `429`, and load-test unrelated latency.
- **Location:** `marilwyd/marilwyd.pony:8`, `marilwyd/sync.pony:114`, `marilwyd/documents.pony:46`, `docs/completion.md:114`, `docs/completion.md:162`, `docs/next-increment.md:46`. **Finding:** [Contracts] Package, sync, login-flow, completion, and roadmap prose describe the obsolete pre-room/pre-crypto system. **Personas:** Editor, API Design, Principles. **Evidence:** The texts variously deny usable Element, rooms/events, login flows, and encryption that current routes and actors implement, while listing completed encryption as a current limitation. **Fix:** Replace milestone snapshots with current contracts and clearly mark or relocate historical material.
- **Location:** `marilwyd/read_bridges.pony:38`, `marilwyd/bridges.pony:76`, `marilwyd/user_link.pony:446`, `marilwyd/link_directory.pony:44`, `marilwyd/link_directory.pony:112`. **Finding:** [Identity] Bridge identity uniqueness is unchecked: templates can collapse or impersonate Matrix identities, and duplicate network names alias ghost and connection keys. **Personas:** Correctness, Security, Wildcard (template/account collision); Wildcard (duplicate-network collision). **Evidence:** `to_matrix` lacks placeholder, legality, injectivity, and namespace checks; `{nick}` can become real `@alice`, constants merge speakers, and duplicate names collapse `LinkDirectory`'s `(user, network.name, channel)` key. **Fix:** Enforce a reserved injective ghost namespace, unique network identities, validated complete localparts, and startup tests for account/template/cross-network collisions.
- **Location:** `marilwyd/room.pony:197`, `marilwyd/room.pony:508`, `marilwyd/rooms.pony:536`. **Finding:** [Delivery] Bridged join reports membership success before installing the outbound carrier. **Personas:** API Design, Principles. **Evidence:** `joined_with` sends `join` then `carry`; `join` callbacks can release another sender before `carry`, while merely reversing messages fails because `carry` ignores nonmembers. **Fix:** Add one atomic room operation that commits member, watcher, and carrier before success, update the sequence prose, and test an immediate callback-triggered send.
- **Location:** `marilwyd/room.pony:232`, `marilwyd/room.pony:569`, `marilwyd/membership_receiver.pony:1`. **Finding:** [Delivery] Membership paths mutate state and report success when event-ID generation fails. **Personas:** API Design. **Evidence:** Invite, `_admit`, leave, and ghost transitions ignore `_append`'s `None`, creating membership/event-history divergence; receivers cannot express `NoEventId`, and creator admission is also unchecked. **Fix:** Mint/build events before state mutation, atomically commit correlated maps/events, extend the refusal union, and inject event-ID generation for deterministic failure tests.
- **Location:** `marilwyd/room.pony:284`, `marilwyd/documents.pony:267`. **Finding:** [Sync] Departures are serialized under `rooms.join`. **Personas:** Correctness. **Evidence:** Leave fans out before removing the member, so the departing device queues the event, while `SyncDocument` has only a hard-coded join branch; terminal bridge departure has no client-side action to mask it. **Fix:** Carry membership transitions explicitly and render departing-room timelines under `rooms.leave`; test HTTP and terminal-bridge departures.
- **Location:** `marilwyd/room.pony:389`, `marilwyd/user_link.pony:613`, `marilwyd/user_link.pony:659`. **Finding:** [Delivery] IRC disconnect can race an accepted room send, yielding Matrix success for a knowingly dropped message. **Personas:** Adversarial. **Evidence:** `Room.send` trusts an asynchronously maintained stalled set and returns an event ID; `UserLink` may already have cleared `_sending`, reaching the explicit loss branch before `carrier_stalled` arrives. **Fix:** Make generation-scoped carrier acknowledgment part of commit and test that every 200 corresponds to IRC acceptance.
- **Location:** `marilwyd/room_directory.pony:59`, `marilwyd/room_directory.pony:172`, `marilwyd/room.pony:81`. **Finding:** [Delivery] Failed native or bridged room initialization leaves broken directory, alias, publication, and bridge mappings registered. **Personas:** Correctness, Adversarial, Principles. **Evidence:** Maps commit before fallible state-event generation and refusal has no directory rollback, so retries see poisoned rooms/aliases; failure is modeled but entropy is not injectable. **Fix:** Prebuild a validated initialization payload or use directory-owned prepare/commit, then inject and test every failure position.
- **Location:** `marilwyd/room_directory.pony:59`, `marilwyd/user.pony:86`, `marilwyd/user.pony:239`, `marilwyd/user.pony:306`, `SECURITY.md:235`. **Finding:** [Resources] Authenticated users can grow permanent room, account-data, and backup structures without quota or reclamation. **Personas:** Performance. **Evidence:** Empty/unpublished rooms remain registered and account-data/backup collections grow monotonically; the documented deployment-rate-limit posture supplies no in-process lifetime bound. **Fix:** Add account/global quotas and rates, reclaim eligible rooms, cap/replace account and backup state, and test quota failure plus reclamation.
- **Location:** `marilwyd/rooms.pony:52`, `marilwyd/rooms.pony:293`. **Finding:** [Parsing] Malformed or non-object `createRoom` bodies become a valid empty request and create a room. **Personas:** Adversarial, API Design. **Evidence:** Parse failure/wrong root returns `CreateRoomRequest(None, None, false, None)`, indistinguishable from valid `{}` at the handler. **Fix:** Preserve a closed malformed result, return `400 M_BAD_JSON` before mutation, and test truncated, empty, scalar, array, and `{}` bodies.
- **Location:** `marilwyd/rooms.pony:580`, `marilwyd/rooms.pony:602`. **Finding:** [Delivery] Ignored room-send `txnId` makes ordinary retries duplicate Matrix and IRC events. **Personas:** Adversarial. **Evidence:** Every identical retry mints and appends a new event; although documented as intentional, a lost success response triggers the protocol failure the idempotency key exists to prevent. **Fix:** Keep a bounded per-device room/type/transaction map of successful commits and test sequential and simultaneous retries.
- **Location:** `marilwyd/session_registry.pony:70`. **Finding:** [Resources] Linear token scans on one actor create a documented quadratic sync-session ceiling. **Personas:** Performance. **Evidence:** Every authenticated request scans all sessions, while periodic sync-auth frequency also grows with sessions; source measurements estimate one saturated core near 7,500 sessions. **Fix:** Use a public random selector for direct lookup plus constant-time secret verification, shard if needed, and benchmark scale.
- **Location:** `marilwyd/session_registry.pony:140`, `marilwyd/user.pony:40`, `marilwyd/user.pony:319`. **Finding:** [Resources] Logged-out/revoked devices remain permanent actors, queues, and fanout targets without quota or expiry. **Personas:** Performance. **Evidence:** Revocation removes only the session; intended resumption keeps devices while all room/account/ephemeral traffic continues to every historical device, so device count and pre-cap mailbox work are unbounded. **Fix:** Separate resumable storage from active fanout, bound/expire devices and total retained events, unregister evictions, and test logout/resume/expiry reclamation.
- **Location:** `marilwyd/sync.pony:182`, `marilwyd/device.pony:239`, `SECURITY.md:170`. **Finding:** [Resources] Concurrent syncs overwrite the sole waiter while displaced handlers, sockets, bodies, and timers remain retained. **Personas:** Adversarial, Security, Performance, Wildcard. **Evidence:** Expiry/abandon recognizes only current `_parked`; no device/global admission bound exists, and checked-in measurements report roughly 147-262 KiB per hold. **Fix:** Enforce bounded concurrency, complete/cancel before replacement or reject excess, drop GET bodies, and test exactly-once termination and timer/connection release.
- **Location:** `marilwyd/to_device_http.pony:126`, `marilwyd/published_keys.pony:110`. **Finding:** [Parsing] `keys/claim` discards the requested algorithm and spends/returns an arbitrary key for unsupported algorithms. **Personas:** Correctness, Security. **Evidence:** Device IDs survive parsing but algorithm names do not, and claims remove from one untyped pool. **Fix:** Partition by validated algorithm, retain it through dispatch, leave unsupported requests empty without spending, and test supported/unsupported/mixed claims.
- **Location:** `marilwyd/to_device_http.pony:126`, `marilwyd/device.pony:97`. **Finding:** [Resources] Any provisioned account can continuously drain another known device's one-time-key pool. **Personas:** Security. **Evidence:** `keys/query` reveals device IDs and each unrestricted cross-account claim removes a capped key; legitimate openness does not bound repeat depletion. **Fix:** Add per-caller/target burst and rate budgets, audit anomalies, and test hostile claims alongside honest replenishment.
- **Location:** `marilwyd/to_device_http.pony:201`, `marilwyd/login_limits.pony:75`. **Finding:** [Parsing] `sendToDevice` bypasses the repository-wide JSON depth guard. **Personas:** Correctness, Adversarial, API Design, Security (defect); Tests (coverage gap). **Evidence:** It enforces only 49,152 bytes then calls recursive `JSONParser.parse`; thousands of nested containers fit, while comparable endpoints use `_ObjectBody`. **Fix:** Parse through `_ObjectBody`, return `M_BAD_JSON`, and add below/at/above-depth socket tests with valid `messages` plus nested padding.
- **Location:** `marilwyd/user_link.pony:670`. **Finding:** [Contracts] `UserLink.deliver` falsely claims only owner events reach it, making its identity-preserving sender guard appear removable. **Personas:** Editor. **Evidence:** `Room._append` sends every event to every carrier; without `event.sender != _user_id`, other members are relayed through the wrong IRC nickname and can loop. **Fix:** Document the actual all-events input and owner-only forwarding invariant.

## Medium
- **Location:** `Makefile:54`; `marilwyd/room.pony:74`, `marilwyd/room.pony:573`, `marilwyd/login_limits.pony:96`, `marilwyd/routes.pony:173`, `marilwyd/keys_http.pony:266`, `marilwyd/to_device.pony:20`, `marilwyd/ghost.pony:108`, `marilwyd/bridged_room_receiver.pony:28`; `marilwyd_test/session_tests.pony:9`, `marilwyd_test/link_tests.pony:9`, `marilwyd_test/link_tests.pony:427`, `marilwyd_test/link_tests.pony:741`, `marilwyd_test/key_http_tests.pony:127`, `marilwyd_test/wake_tests.pony:229`, `marilwyd_test/wake_tests.pony:496`, `marilwyd_test/main.pony:449`, `marilwyd_test/main.pony:643`. **Finding:** [Contracts] Production, test, and build prose narrates coverage, mutations, benchmarks, CI behavior, and implementation history rather than durable invariants. **Personas:** Editor. **Evidence:** All cited instances contain shelf-life/history clauses that can stale independently and bury useful failure, ordering, fixture, or timeout facts. **Fix:** Retain only current guarantees and non-obvious ordering/fixture/outer-timeout rationale; remove coverage inventories, mutation outcomes, prior versions, and CI history.
- **Location:** `SECURITY.md:373`, `marilwyd/published_keys.pony:11`. **Finding:** [Contracts] SECURITY gives the obsolete 131,072-byte key-upload limit rather than 49,152 and wrongly describes it as smaller than to-device. **Personas:** Correctness, Principles, Editor. **Evidence:** Source identifies 131,072 as the old unreachable value, so current capacity/DoS prose is false. **Fix:** State current constants/transport relationship, preferably deriving operational text from definitions.
- **Location:** `docs/architecture.md:770`. **Finding:** [Contracts] The restart invariant says accounts disappear, conflating volatile runtime state with credential-backed identity. **Personas:** Editor. **Evidence:** Credentials recreate configured identities; sessions, rooms, invitations, queues, keys, and account data disappear. **Fix:** State that runtime account state is volatile while configured identities reload.
- **Location:** `marilwyd/create_room_request.pony:24`, `marilwyd/room_event.pony:32`, `marilwyd/to_device.pony:52`. **Finding:** [Contracts] Public constructors bypass claimed validation and accept arbitrary encryption/pre-rendered JSON strings. **Personas:** Principles. **Evidence:** Normal boundaries validate algorithms/use `JSONPrinter`, but sibling-test-driven public constructors can form values that render malformed wire JSON. **Fix:** Repair the executable/test package boundary or expose checked factories with private unchecked constructors.
- **Location:** `marilwyd/device.pony:459`, `marilwyd/documents.pony:210`. **Finding:** [Resources] Initial sync duplicates preflight slices and scans all state/ephemeral entries once per room. **Personas:** Performance. **Evidence:** Answer construction rebuilds arrays used only to decide parking, then performs approximately `O(R * (S + E))` filtering and repeated room-ID cloning. **Fix:** Carry/index room-grouped state once, reuse slices or cheap presence checks, and benchmark near-linear scaling.
- **Location:** `marilwyd/event_receiver.pony:3` and the receiver-interface family listed in Editor evidence. **Finding:** [Contracts] Receiver docstrings restate receiving anthropomorphically while omitting useful callback alternatives/timing guarantees. **Personas:** Editor. **Evidence:** Two dozen "waiting to be told/handed" openers force callers to inspect behaviors for actual result semantics. **Fix:** Rewrite each as a concrete callback-result contract, asserting exactly-once/order only where supported.
- **Location:** `marilwyd/room.pony:439`, `marilwyd/user.pony:319`. **Finding:** [Resources] Redundant receipt/typing requests rebuild and fan out full-room snapshots. **Personas:** Performance. **Evidence:** Identical updates and failed additions at the typist cap still allocate and publish through every user/device, causing real cost but no demonstrated behavioral failure. **Fix:** Suppress no-op updates, coalesce bounded bursts, use deltas where valid, and test zero extra delivery.
- **Location:** `marilwyd/room_state.pony:52`, `marilwyd/room_state.pony:66`, `marilwyd/room_state.pony:121`, `marilwyd/room.pony:284`, `marilwyd/room.pony:562`, `marilwyd/main.pony:173`, `marilwyd/documents.pony:12`, `marilwyd/documents.pony:39`, `marilwyd/device_id.pony:24`, `marilwyd/device_id.pony:46`. **Finding:** [Contracts] Recorded representative public methods lack adjacent API documentation. **Personas:** Principles. **Evidence:** These actionable examples include state transitions/predicates, callbacks, renderers, and ID operations whose preconditions/results are absent from generated docs; no unsupported repository-wide count is asserted. **Fix:** Document these methods first and redesign visibility for declarations public only to sibling tests.
- **Location:** `marilwyd/routes.pony:13`, `README.md:325`. **Finding:** [Contracts] `hobby#1` is an unresolved internal marker. **Personas:** Editor. **Evidence:** Neither location supplies a URL, repository, or definition, and surrounding prose already explains the wildcard-root workaround. **Fix:** Replace with a descriptive local label or exact upstream link, then remove the redundant README marker.
- **Location:** `marilwyd_test/key_tests.pony:233`, `marilwyd_test/key_tests.pony:248`. **Finding:** [Contracts] Two user-signing tests report the opposite behavior in their names. **Personas:** Tests. **Evidence:** The withholding test says owner delivery and the owner-delivery test says withholding, so failures direct maintainers to the wrong branch. **Fix:** Swap the two names or class names.

## Low
- **Location:** `README.md:3`, `SECURITY.md:3`, `docs/architecture.md:3`, `docs/next-increment.md:3`. **Finding:** [Contracts] Markdown prose is systematically hard-wrapped at code width. **Personas:** Principles. **Evidence:** Paragraph edits reflow unrelated lines and add review/merge noise, but nothing is false or missing. **Fix:** Reflow ordinary paragraphs in a documentation-only change while preserving semantic list/table/quote/fence breaks.
- **Location:** `marilwyd/device.pony:343`. **Finding:** [Delivery] A currently unreachable to-device merge fallback returns truncated success rather than failing loudly. **Personas:** Principles. **Evidence:** The impossible no-candidate branch `break`s and ignored index failures advance `taken`; current invariants prevent normal reachability. **Fix:** Use a private mort-style `_Unreachable` or encode candidate existence in the result type.
- **Location:** `marilwyd/room.pony:19`. **Finding:** [Resources] Exclusively owned `RoomState` uses `let` rather than `embed`. **Personas:** Principles. **Evidence:** Each room constructs it, exposes no alias, and embeds its other owned mutable collections, leaving one avoidable allocation/indirection. **Fix:** Declare `embed _state: RoomState` and recompile.

## Passes
- Full mode used all nine personas for this one-shot repository review. Shared baseline: ponyc 0.69.1 compiled, all 260 tests passed, and pony-lint passed both packages.
- Strong areas include typed IDs/refusals, immutable actor-transfer values, authenticated ownership checks, constant-time secret comparison, JSON/IRC escaping, TLS verification, and bounded isolated queues.
- Tests are broad and integration-heavy; most structured endpoints enforce both byte and JSON-depth limits, making the reported exceptions localized.

## Uncertainties
- Event-ID/CSPRNG failure is not injectable; inconsistent states follow from control flow, while operational frequency depends on the entropy provider.
- Invitation rates, typing UI persistence, bridge interleavings, and concurrent-sync final responses were not live-tested; the underlying state/order failures are direct code evidence.
- Symlink serving and displaced-request outcomes partly depend on untracked hobby/stallion/lori behavior; the symlink claim uses the checked-in probe rather than independent dependency verification.
- `docs/completion.md` may have been intended as history, but it is unmarked and uses present-tense operational claims.
